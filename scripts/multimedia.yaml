#Script

watch_tv:
  sequence:
    - service: remote.turn_on
      entity_id: remote.harmony_hub
      data:
        activity: "Watch TV"

watch_chromecast:
  sequence:
    - service: remote.turn_on
      entity_id: remote.harmony_hub
      data:
        activity: "Watch Chromecast"

turn_off_tv:
  sequence:
    - service: remote.turn_on
      entity_id: remote.harmony_hub
      data:
        activity: "PowerOff"  